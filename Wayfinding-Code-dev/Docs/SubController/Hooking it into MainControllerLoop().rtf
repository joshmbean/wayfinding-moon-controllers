{\rtf1\ansi\ansicpg1252\cocoartf2821
\cocoatextscaling0\cocoaplatform0{\fonttbl\f0\fswiss\fcharset0 Helvetica;}
{\colortbl;\red255\green255\blue255;}
{\*\expandedcolortbl;;}
\margl1440\margr1440\vieww11520\viewh8400\viewkind0
\pard\tx720\tx1440\tx2160\tx2880\tx3600\tx4320\tx5040\tx5760\tx6480\tx7200\tx7920\tx8640\pardirnatural\partightenfactor0

\f0\fs24 \cf0 void MainControllerLoop() \{\
  static uint32_t lastsweep = 0;\
  static String lastMode = " ";\
  static bool override = false; // was override on last run?\
  static bool musicInitialised = false;\
\
  uint32_t col;\
  uint8_t pressure;\
\
  if (lastMode == " ") \{\
    // we've just booted up\
    lastMode = "Debug";\
    I2C_send_mode((uint8_t) 0x00U, "Debug");\
    consolePrintln("Bootup Complete, Setting Calibration Mode");\
  \}\
\
  // --- existing mode override/user logic stays as-is ---\
  // (this is your existing code that sets lastMode & globalConfig.modeUser\
  // and calls I2C_send_mode(...) when modes change)\
\
  // ... existing code ...\
\
  // After that logic, globalConfig.modeUser will be the active mode string.\
  // We can now run per-mode behaviours:\
\
  if (globalConfig.modeUser == "Music") \{\
    if (!musicInitialised) \{\
      MusicMode_init();\
      musicInitialised = true;\
    \}\
    MusicMode_update();\
  \} else \{\
    // If we leave Music mode, reset flag so next time we re-init\
    musicInitialised = false;\
  \}\
\
  // Keep any existing debug/sweep/Simon code below as needed.\
  // For example, they currently do some periodic sweep & Simon handling.\
\}\
}